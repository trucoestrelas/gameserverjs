class Carta {
    constructor(naipe, valor, forca) {
        this.naipe = naipe;
        this.valor = valor;
        this.forca = forca;  // Definimos a forca da carta no truco.
    }
}

module.exports = Carta;
